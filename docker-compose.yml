version: '3.8'
services:
  frontend:
    container_name: EMS-Admin-External
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    ports:
      - '5002:5002'
    volumes:
      - ./:/app
      - /app/node_modules
