export enum UserStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  PENDING = 'pending',
  BLOCKED = 'blocked',
  UNSIGNED = 'unassigned',
}

export enum UserSummarizeType {
  TOTAL = 'total',
}
